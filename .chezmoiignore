# Ignore patterns for `chezmoi`

# gitignore
.gitignore

# python
__pycache__

# Repository specific files
chezmoi.toml
README.md
LICENSE
assets/
justfile
temp.py
docs/
.env
CLAUDE.md
# Symlink to CLAUDE.md; without this chezmoi follows it and deploys the
# repo guide to ~/AGENTS.md, where Codex/OpenCode read it as global rules.
AGENTS.md
pkgs/
misc/

# Editconfig
.editorconfig

# Makefile
Makefile

# bin
bin/

# Platform-specific: Linux-only configs (ignore on macOS)
{{- if eq .chezmoi.os "darwin" }}
.config/niri
.config/hypr
.config/waybar
.config/swaync
.config/wofi
.config/walker
.config/wlr-which-key
.config/ulauncher
{{- end }}

# Platform-specific: macOS-only configs (ignore on Linux)
{{- if eq .chezmoi.os "linux" }}
.aerospace.toml
.config/sketchybar
{{- end }}

# misc
tests/
.config/sesh/sesh.toml
