# Handy's recording overlay uses gtk-layer-shell on Linux, but under niri the
# layer surface maps on the overlay layer and commits nothing visible. Falling
# back to a regular window makes it render; niri floats it via a window-rule
# matching app-id "handy" / title "Recording".
HANDY_NO_GTK_LAYER_SHELL=1
