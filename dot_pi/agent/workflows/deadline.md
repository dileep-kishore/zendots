Complete this task using the smallest defensible path to done:

$ARGUMENTS

Avoid optional polish, broad rewrites, speculative improvements, and exhaustive research. Use one writer only. If delegation materially helps, use at most one worker and one independent verifier; do not create a large fan-out even though the global concurrency ceiling is 8.

Run the cheapest direct check that proves the requested behavior. If full validation is unavailable, clearly state what remains unknown instead of compensating with more scope. Report the result, evidence, and residual risk concisely. Do not commit or push unless I explicitly ask.
