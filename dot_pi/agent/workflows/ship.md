Implement this approved scope end to end:

$ARGUMENTS

Before editing, restate the validation contract and inspect the load-bearing files. Keep one writer in the active cwd/worktree. Make surgical changes using existing patterns and do not expand scope.

After implementation, run focused validation. For non-trivial work, use one pi-subagents workflowScript with stable keys to launch distinct fresh-context read-only reviewer/verifier lanes, with up to 8 children concurrently. Synthesize only evidence-backed findings, then use one writer for accepted fixes and recheck affected behavior.

Report changed files, checks and outcomes, residual risks, and anything deferred. Do not commit or push unless I explicitly ask.
