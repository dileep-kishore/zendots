Run a read-only, adversarial audit of:

$ARGUMENTS

Design one pi-subagents workflowScript. Partition the scope into genuinely distinct lanes with stable keys and run up to 8 children concurrently. Give each child a specific target, evidence contract, and decision. Prefer fresh-context scout, reviewer, and verifier agents; do not use writers.

Before synthesis, mechanically check completeness: expected count or scope, returned identifiers, duplicates, missing items, failed lanes, and unknowns. Retry or explicitly preserve gaps; never silently subset. Use a separate adversarial verification pass for candidate findings.

Return confirmed findings grouped by severity, rejected/duplicate findings, coverage, exact evidence, smallest fixes, and a prioritized recommendation. Do not modify files.
