#!/bin/sh

# Bound the connect phase on macOS/BSD netcat so ssh Match exec never hangs.
# The bastion host is only reachable on the ORNL network or VPN.
exec /usr/bin/nc -G 1 -z bstn-wks-gate.ornl.gov 22 >/dev/null 2>&1
